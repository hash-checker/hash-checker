<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout android:id="@+id/button_generate"
        style="@style/AppSelectableLinearLayout"
        android:layout_marginTop="@dimen/common_margin">

        <ImageView android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:srcCompat="@drawable/ic_generate" />

        <TextView style="@style/AppBottomTextView"
            android:text="@string/generate" />

    </LinearLayout>

    <LinearLayout android:id="@+id/button_compare"
        style="@style/AppSelectableLinearLayout">

        <ImageView android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:srcCompat="@drawable/ic_compare" />

        <TextView style="@style/AppBottomTextView"
            android:text="@string/compare" />

    </LinearLayout>

</LinearLayout>
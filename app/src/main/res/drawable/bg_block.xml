<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray" />

    <padding
        android:bottom="@dimen/round_padding"
        android:left="@dimen/round_padding"
        android:right="@dimen/round_padding"
        android:top="@dimen/round_padding" />

    <corners
        android:bottomLeftRadius="@dimen/round_padding"
        android:bottomRightRadius="@dimen/round_padding"
        android:topLeftRadius="@dimen/round_padding"
        android:topRightRadius="@dimen/round_padding" />

</shape>